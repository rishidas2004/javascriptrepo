// arrow function :- same as like anonymous function but
                    // we dont write function in it
                    // we use "=>" arrow function instead of {}

let sumAddition = (x,y) =>{
    return x+y;
}

console.log(sumAddition(12,10));